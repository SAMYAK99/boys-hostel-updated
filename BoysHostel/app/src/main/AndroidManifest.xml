<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.boyshostel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".timing"></activity>
        <activity android:name=".mtiming" />
        <activity android:name=".mmenu" />
        <activity android:name=".mess" />
        <activity android:name=".fees" />
        <activity android:name=".dean" />
        <activity android:name=".anti" />
        <activity android:name=".activity3rd" />
        <activity android:name=".activity2nd" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>